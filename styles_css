/* Google Fonts */
@import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&family=Nunito:wght@300;400;700&display=swap');

body {
    margin: 0;
    font-family: 'Poppins', sans-serif;
}

.hero {
    position: relative;
    height: 100vh;
    background-image: url("WhatsApp Image 2025-12-05 at 04.59.19_0e53d238.jpg");
    background-size: cover;
    background-position: center top;
    background-repeat: no-repeat;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
}

/* Gradient Overlay */
.hero .overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(
        to bottom right,
        rgba(40, 80, 200, 0.55),
        rgba(110, 45, 190, 0.55),
        rgba(180, 150, 255, 0.55)
    );
    backdrop-filter: blur(2px);
}

/* Hero Content */
.hero-content {
    position: relative;
    z-index: 2;
    color: white;
    max-width: 800px;
    padding: 0 20px;
}

.hero-content h1 {
    font-size: 3.5rem;
    font-family: 'Nunito', sans-serif;
    font-weight: 700;
    margin-bottom: 10px;
}

.hero-content h1 span {
    background: linear-gradient(to right, #b3c7ff, #d5b5ff);
    -webkit-background-clip: text;
    color: transparent;
}

.hero-content p {
    font-size: 1.3rem;
    font-weight: 300;
    margin-bottom: 25px;
}

.btn {
    background: linear-gradient(to right, #657EFF, #A175FF);
    color: white;
    padding: 12px 30px;
    border-radius: 30px;
    text-decoration: none;
    font-weight: 600;
    font-family: 'Nunito', sans-serif;
    transition: 0.2s ease-in-out;
}

.btn:hover {
    transform: translateY(-3px);
    box-shadow: 0 8px 20px rgba(120, 80, 255, 0.4);
}

/* Mobile Optimization */
@media(max-width: 768px) {
    .hero-content h1 {
        font-size: 2.5rem;
    }
    .hero-content p {
        font-size: 1rem;
    }
}
